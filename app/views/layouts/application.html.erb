<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "DisasterWatch" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>

    <script src="http://d3js.org/d3.v3.min.js" charset="utf-8"></script>
    <script src="http://d3js.org/topojson.v1.min.js"></script>
  </head>

  <body>

    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><a href="#"><%= link_to "HOME",root_path %></a></h1>
        </li>
         <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
        <li class="toggle-topbar menu-icon"><a href="#"></a></li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <% if user_signed_in? %>
            <li class="active"><a href="#"><%= link_to "PROFILE", user_path(current_user) %></a></li>
          <% else %>
            <li class="active"><a href="#"><%= link_to "Sign Up", new_user_registration_path %></a></li>
          <% end %>
          
          <li class="has-dropdown">
            <% if user_signed_in? %>
              <a href="#"><%= link_to "Sign Out", user_path(destroy_user_session_path), method: :delete %></a>
            <% else %>
              <a href="#"><%= link_to "Sign In",  new_user_session_path %></a>
            <% end %>
            
<!--             <ul class="dropdown">
              <li><a href="#">First link in dropdown</a></li>
            </ul> -->

          </li>
        </ul>

        <!-- Left Nav Section -->
        <ul class="left">
          <li><a href="#"><%= link_to "MAP", root_path %></a></li>
          <li><a href="#"><%= link_to "ALL ORGANIZATIONS", organizations_path %></a></li>
          <li><a href="#"><%= link_to "ALL NEWS", updates_path %></a></li>
          <% if user_signed_in? %>
            <li><a href="#"><%= link_to "MY NEWSFEED", countries_path %></a></li>
          <% end %>
        </ul>
      </section>
    </nav>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>